// AddressSidebar is now deprecated — address UI moved into CartSidebar.
// Keep a tiny module to avoid import errors in case any file still references it.
export default function AddressSidebarStub() {
  return null;
}
